package io.dindinw.raft.api.rest;

/**
 * Restful API (by using javax.ws.rs). it's just a easy choice, so that a abstract layer should here to
 * separate the HTTP layer with real impl. we might need to replaced the http layer by using the aysc
 * framework like netty or vert.x
 */
public class RaftResource {
}
